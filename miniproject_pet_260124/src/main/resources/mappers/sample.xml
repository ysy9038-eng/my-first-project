<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bbs.dao.BoardDao">


    <select id="getBoardList" resultType="BoardVo">
        SELECT * FROM board ORDER BY b_idx DESC
    </select>

    <select id="getBoardDetail" parameterType="int" resultType="BoardVo">
        INSERT INTO board (b_idx, b_title, b_content, b_writer, b_regdate)
        VALUES (seq_board_idx.nextVal, #{b_title}, #{b_content}, #{b_writer}, sysdate)
    </select>

</mapper>
 
  













